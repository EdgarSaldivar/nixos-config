# Build artefacts
build.log
result
result-*
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Never commit decrypted secrets. Encrypted sops files ARE tracked
# (secrets/*.yaml are ciphertext); these are the plaintext escape hatches.
*.plain.yaml
*.dec.yaml
secrets/*.plain
.seats/review/
